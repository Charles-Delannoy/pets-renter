<h1>Pets</h1>

<ul>
  <% @pets.each do |pet| %>
  <li>
    <%= pet.name %> - <%= pet.pet_type %> - <%= pet.race %>
    <%= cl_image_tag pet.photo.key, height: 400, width: 400, crop: :fill %>
  </li>
  <% end %>
</ul>
